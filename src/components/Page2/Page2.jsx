import Authorization from "./JS/Authorization";
import "./Page2.css";

const Page2 = ({ dataCloud, jwt, firstVisit }) => {
    return (
        <>
            <Authorization />
        </>
    );
};

export default Page2;